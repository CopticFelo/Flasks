import SwiftUI

struct TableDownloadView: View {
    let downloadURL: String
    var body: some View {
        HStack {
            Spacer()
            Button {
            } label: {
                Image(systemName: "square.and.arrow.down")
            }
        }
    }
}
